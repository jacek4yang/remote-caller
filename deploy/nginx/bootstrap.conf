# Temporary HTTP-only configuration used to obtain the first certificates.
server {
    listen 80;
    listen [::]:80;
    server_name call.example.com;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / { return 404; }
}
